import Timer from 'easytimer.js';
import Lobby from '../../../interfaces/Lobby';

/**
 *  Atualiza o timer de uma sala. Isso deve ser feito devido à saída de jogadores da partida, fazendo com que os eventos programados
 *  pelo timer precisem ser atualizados.
 *
 *  @param lobby lobby que possui o timer a ser atualizado
 *  @returns o timer atualizado
 */

export function updateTimer(lobby: Lobby): Timer {

}
